#!/bin/sh

flags=$(sort -R words-that-work-in-ls.txt | head -n1)
echo ls -$flags
ls -$flags

# The words-that-work-in-ls.txt was generated by Thomas Levine like so

# rm ls-corncob-success.txt; echo '#!/bin/sh' > ls-corncob.sh && sed -e 's/.$//' -e 's/^\(.*\)/ls -\1 \&\& echo \1 >> ls-corncob-success.txt/' corncob_lowercase.txt >> ls-corncob.sh
# (This depends on a file called corncob_lowercase.txt with a list of words.)

